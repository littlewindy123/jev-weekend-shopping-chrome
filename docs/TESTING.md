# 测试范围

版本：0.4.3。记录日期：2026-09-21。

## 已执行

- `node --test tests/*.test.js`：42 项通过。
- `node tests/native-page.mjs`：11 组通过，使用模拟商品和模型回复，检查与实际观察相符的京东卡片结构、滚动、复用、盖章和状态提示。
- `node tests/browser.mjs`：14 组通过。真实加载 MV3 扩展，设置持久化实际运行；连接结果和商品分类使用明确标注的模拟响应。
- 官方 JEV `jev-1.13.0` 实际调用：3 个虚构排班控制样本和 3 个公开商品标题。单休→盖章、双休→不盖章、大小周→盖章；标题样本获得有效二选一结果。没有准确率结论。

## 未作保证

- 没有企业真实排班标注集，不能报告商家双休识别准确率。
- 淘宝真实页面完整流程尚未验收。
- 京东真实页面结构已核对，但不以模拟页面截图冒充实站盖章成功。
- JEV 输出会受文字表述、模型版本和输入信息影响；商品类别不是企业排班证明。

## 可选真实接口检查

```bash
node tests/official-live.mjs --binary-check
```

此命令会提示在终端隐藏输入 Key，实际发送 6 个小样本并消耗账户调用额度。结果写入被 Git 忽略的 `verification/`，不记录 Key 或服务原始错误。

普通单元测试和浏览器回归不需要真实 Key，也不调用真实模型。

## 发布时的检查

公开内容只包含代码、测试、文档和无密钥的设置面板图。个人路径、账户资料、临时日志和排查期间的原始记录不作为公开交付内容。安装更新后，必须刷新商城页面，让内容脚本使用当前版本。
